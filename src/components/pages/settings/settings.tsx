import React, { FunctionComponent, ReactElement } from 'react';

import { Container, Heading } from '../../shared';

export const SettingsPage: FunctionComponent = (): ReactElement => (
  <Container>
    <Heading level="h1" title="Settings" />
  </Container>
);
